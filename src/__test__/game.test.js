const {
    BowlingGame
} = require('../game');


describe('check game has players', () => {
    xit('check the game has at least 1 player', () => {
        const game = new BowlingGame();
        game.
        expect().toEqual(0,10);
    })
})
